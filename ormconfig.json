{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "admin",
  "password": "admin",
  "database": "wishlistr",
  "synchronize": true,
  "logging": false,
  "entities": ["src/model/*.ts", "modules/**/model/*.ts"],
  "migrations": ["src/migration/*.ts", "src/migration/*.sql"],
  "cli": {
    "migrationsDir": "migration"
  }
}